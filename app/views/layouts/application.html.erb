<!DOCTYPE html>
<html>
  <head>
    <title>StimulusHandbook</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <% # Toastify JS CSS %>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body data-controller="toastify" data-flash-messages="<%= JSON.dump(flash.to_a) %>" class="container mx-auto max-w-[1024px]">
    <%= render partial: "layouts/navbar" %>
    
    <main class="rounded-lg py-5">
      <%= yield %>
    </main>
  </body>
</html>
